# Play Console 진행 런북 (2026-08-04)

개발자 계정 승인 직후부터 내부 테스트 출시·결제 연결까지의 순서다. **콘솔 화면 순서
그대로** 적었고, 물어보는 항목의 답을 미리 확정해 두었다. 근거는
`docs/play-store-submission.md`(데이터 안전·등급·등록정보 초안)와
`docs/entitlement-schema-draft.md`(결제).

표기: **[결정]** 이 문서에서 확정한 답 · **[사용자]** 내가 대신할 수 없는 입력 ·
**[확인 필요]** 콘솔 화면에서 실제 문항을 보고 판단

---

## 0. 시작 전 — 계정 유형부터 본다

**설정 → 개발자 계정 → 계정 정보**에서 계정 유형을 확인한다. 이 값 하나가 출시
경로를 가른다.

| 계정 유형 | 프로덕션까지의 경로 |
| --- | --- |
| **조직(Organization)** | 내부 테스트 → 바로 프로덕션 신청 가능 |
| **개인(Individual)** | 2023-11 이후 만든 계정은 **비공개 테스트에 테스터 12명이 14일 연속** 참여해야 프로덕션 신청이 열린다 |

**[사용자]** 어느 쪽인지 알려 달라. 개인이면 12명을 모으는 것이 실질적으로 가장 긴
일정이라, 내부 테스트 업로드 직후에 비공개 테스트를 함께 열고 14일 시계를 먼저
돌리는 편이 낫다.

**[확인 필요]** 사업자등록을 했더라도 Play 계정을 개인으로 만들었으면 개인 계정이다.
계정 유형은 나중에 변경 요청이 가능하지만 즉시 되지 않는다.

---

## 1. 앱 만들기

**모든 앱 → 앱 만들기**

| 항목 | 값 |
| --- | --- |
| 앱 이름 | `On My Way - 목표 실행 코치` **[결정]** (19자, 제한 30자) |
| 기본 언어 | 한국어 (대한민국) |
| 앱 또는 게임 | 앱 |
| 무료 또는 유료 | **무료** **[결정]** — 구독은 인앱 상품이다 |

> **무료→유료 전환은 불가능하다**(유료→무료만 된다). 구독 모델이므로 앱 자체는 무료가
> 맞다.

선언 체크박스(개발자 프로그램 정책·미국 수출법)는 **[사용자]** 가 읽고 동의한다.

---

## 2. 앱 설정 — 선언 항목

대시보드의 「앱 설정」 목록을 위에서부터 채운다.

### 2.1 앱 액세스 **[사용자 입력 필요]**

**답: "일부 기능이 제한됨"**

앱은 로그인 없이도 「앱 구경하기」로 화면을 볼 수 있지만, AI 기능·기록 동기화는 소셜
로그인이 필요하다. 구글은 이 경우 리뷰어용 자격증명을 요구한다.

- **[사용자]** 일회용 구글 계정을 하나 만들어 그 이메일·비밀번호를 이 칸에 넣는다.
  **나는 자격증명을 대신 입력하지 않는다.**
- 소셜 로그인만 지원하므로 카카오·네이버보다 **구글 계정**이 리뷰어에게 가장 쉽다.
- 안내문에 넣을 문구:

```
1. 앱을 실행하고 우측 상단 메뉴 → 로그인을 누릅니다.
2. "Google로 계속하기"를 선택하고 아래 계정으로 로그인합니다.
3. 로그인하면 무료 체험이 자동 시작되어 AI 기능(올리와 대화, 계획 수정 등)을
   사용할 수 있습니다.
※ 로그인 없이도 "앱 구경하기"로 목표 만들기와 오늘 화면을 확인할 수 있습니다.
```

### 2.2 광고

**답: 아니요, 앱에 광고가 없습니다** **[결정]** — 광고 SDK가 0개다(제품 HTML에 외부
`<script src>` 없음).

### 2.3 콘텐츠 등급 (IARC 설문)

이메일 입력 후 카테고리는 **「유틸리티, 생산성, 커뮤니케이션, 기타」**를 고른다.

| 문항 | 답 |
| --- | --- |
| 폭력 / 성적 콘텐츠 / 욕설 / 약물·주류·담배 / 도박 / 공포 | 전부 **없음** |
| 사용자 간 상호작용(채팅·소통) | **없음** — 올리는 AI다. 유저끼리 소통하는 경로가 없다 |
| 사용자 위치 공유 | 없음 |
| 개인정보 공유 | **예** — 소셜 로그인으로 이름·이메일을 받는다 |
| 디지털 구매 | **예** — Pro 구독 |

**예상 등급: 전체이용가(3+)**

**[확인 필요]** 만세력(생년월일·출생시각으로 천간·지지를 계산해 계획 문구에 반영,
`script.js`의 `calculateSimpleManse`) 관련 문항이 나오면 "운세·점술 콘텐츠"로 답한다.
등급을 올리는 요소는 아니지만 숨기면 허위 신고가 된다.

### 2.4 타겟층 및 콘텐츠

| 항목 | 답 |
| --- | --- |
| 타겟 연령대 | **18세 이상만 선택** **[결정]** |
| 아동에게 어필하는 요소가 있는가 | 아니오 |

**18세 이상만 고르는 이유**: 18세 미만을 하나라도 선택하면 **Families 정책**이
적용되어 아동 대상 설계 검토·SDK 심사·광고 ID 제약이 함께 붙는다. 개인정보처리방침
§5가 이미 만 14세 미만을 배제하고 있어 모순도 없다. 타겟층은 "설계 의도"를 묻는 것이지
실제 사용자 나이를 제한하는 장치가 아니다.

**[확인 필요]** 올리 캐릭터 때문에 "아동에게 어필" 판정을 받을 여지가 있다. 앱 콘텐츠
자체는 성인 대상 목표관리이므로 답은 "아니오"가 맞고, 구글이 재검토를 요청하면 스토어
설명(계획·구독·AI 코칭)을 근거로 답한다.

### 2.5 나머지 선언

| 항목 | 답 |
| --- | --- |
| 뉴스 앱 | 아니오 |
| 코로나19 접촉 확인 앱 | 아니오 |
| 데이터 보안 | §2.6 |
| 정부 앱 | 아니오 |
| 금융 기능 | **아니오** — 구독 결제는 금융 상품이 아니다 |

### 2.6 데이터 보안 (Data safety)

`docs/play-store-submission.md` §1의 판정 결과다. 그 문서에 항목별 코드 근거가 있다.

**공통 답**: 전송 중 암호화 **예** · 데이터 삭제 요청 가능 **예**
(`https://onmyway.olivenrich.com/delete-account` — 2026-08-04 로그인 없이 200 응답 확인)

**제3자와 공유: 아니오** — Cloudflare·OpenAI·소셜 로그인 제공자·Google은 전부 수탁
관계다.

| 카테고리 | 유형 | 수집 | 필수 | 목적 |
| --- | --- | --- | --- | --- |
| 개인 정보 | 이름 | 예 | 필수 | 앱 기능, 계정 관리 |
| 개인 정보 | 이메일 주소 | 예 | 선택 | 앱 기능, 계정 관리, 고객 지원 |
| 개인 정보 | 사용자 ID | 예 | 필수 | 앱 기능, 계정 관리, 사기 방지 |
| 개인 정보 | 기타 정보 (생년월일·출생시각·MBTI) | 예 | 선택 | 앱 기능(개인화) |
| 금융 정보 | 구매 내역 | 예 | 필수 | 앱 기능, 사기 방지 |
| 금융 정보 | 사용자 결제 정보 | **아니오** | — | 카드 정보를 받지 않는다 |
| 앱 활동 | 기타 사용자 제작 콘텐츠 | 예 | 선택 | 앱 기능(기기 간 동기화) |
| 앱 활동 | 앱 상호작용 | **아니오** | — | 퍼널은 날짜별 카운터뿐, 개인별 레코드가 없다 |
| 메시지 | 인앱 메시지 | **아니오** | — | 올리와의 대화는 기기에만 남는다 |
| 기기 또는 기타 ID | 기기 ID | **아니오** | — | 푸시 미구현, IP는 레이트리밋 키로만 쓰고 저장하지 않는다 |

**남은 판단 둘** (제출 전에 닫는다):
- **프로필 사진**: 소셜 제공자의 프로필 이미지 **URL 문자열**만 저장한다(이미지 파일을
  받지 않는다). **[결정]** "사진"이 아니라 **개인 정보 → 기타 정보**로 신고한다.
- **진단 데이터**: 크래시 SDK가 없고 서버 `console.error`만 Cloudflare 로그로 간다.
  **[확인 필요]** Cloudflare Workers Logs 보존 설정을 확인해야 한다. 관측성을 켜지
  않았다면 **아니오**.

### 2.7 정부 규정 준수 (한국)

**[사용자]** 사업자등록번호·통신판매업 신고번호·대표자명·사업장 주소·연락처를
입력한다. 등록 완료된 정보를 그대로 넣으면 된다.

---

## 3. 스토어 등록정보

| 항목 | 값 |
| --- | --- |
| 앱 이름 | `On My Way - 목표 실행 코치` |
| 간단한 설명 (80자) | `목표를 직접 계획으로 만들고, 매일의 실행을 AI 코치 올리와 함께 이어가세요.` |
| 앱 아이콘 | `icon-512.png` |
| 그래픽 이미지 | `feature-1024x500.png` |
| 휴대전화 스크린샷 | `screenshot-1~4` (4장) |
| 앱 카테고리 | **생산성** **[결정]** |
| 이메일 | `support@olivenrich.com` |
| 웹사이트 | `https://onmyway.olivenrich.com` |
| 개인정보처리방침 | `https://onmyway.olivenrich.com/privacy.html` |

파일 위치: `C:\Users\dongh\Downloads\on-my-way-store-assets\`

**자세한 설명**은 `docs/play-store-submission.md` §4의 문안을 쓰되 **인쇄·PDF 줄은
뺀다** — Android WebView는 `window.print()`를 구현하지 않아 그 기능이 조용히
실패한다(`docs/native-print-bridge.md`). 작동하지 않는 기능을 유료 혜택으로 광고하면
표시광고법 문제가 된다. 최종 문안은 §7에 붙였다.

---

## 4. 내부 테스트 트랙 출시

**테스트 및 출시 → 테스트 → 내부 테스트 → 새 버전 만들기**

| 항목 | 값 |
| --- | --- |
| 앱 번들 | `C:\Users\dongh\Downloads\on-my-way-0.1.0-vc2.aab` |
| Play 앱 서명 | 첫 업로드 시 자동 등록 — 우리 PKCS12는 **업로드 키**가 된다 |
| 출시명 | `0.1.0 (2)` |
| 출시 노트 | `첫 내부 테스트 빌드입니다.` |
| 테스터 | 본인 이메일로 목록 하나 만들기 |

**vc1이 아니라 vc2를 올린다** — vc1은 런처 아이콘이 Capacitor 기본 로봇이다.

내부 테스트는 심사 대기가 없어 몇 분 내 링크가 나온다. 그 링크로 실기기에 설치해
**로그인 → 체험 시작 → 올리 대화 → 기록 내보내기**를 한 번 돌리는 것이 다음 검증이다.

---

## 5. 결제 (Play Billing)

앱이 만들어진 뒤에만 가능하다. **내부 테스트 업로드를 먼저 끝내야** 구독 상품을 만들 수
있다(활성 버전이 하나는 있어야 한다).

### 5.1 구독 상품

**수익 창출 → 구독 → 구독 만들기**

| 항목 | 값 |
| --- | --- |
| 제품 ID | `pro_monthly` **[결정] — 만든 뒤 변경 불가** |
| 이름 | `On My Way Pro` |
| 기본 요금제 ID | `monthly` **[결정] — 변경 불가** |
| 갱신 유형 | 자동 갱신 |
| 결제 주기 | 1개월 |
| 가격 (대한민국) | `₩3,900` (`plan-policy.mjs`의 `priceKRW`와 일치) |
| 무료 체험 | **제공하지 않음** **[결정]** |

**무료 체험을 스토어에 만들지 않는 이유**: 우리 체험은 서버가 관리한다(가입 시 시작,
계정당 1회, 남용 마커까지). 스토어 체험을 겹치면 체험 상태가 두 곳에 생기고 어느 쪽이
진실인지 물어야 한다 — `docs/entitlement-schema-draft.md` §0이 경고하는 형태다.

### 5.2 API 액세스 (구매 검증용)

**설정 → API 액세스**

1. Google Cloud 프로젝트를 새로 만들어 연결
2. 서비스 계정 만들기 → **JSON 키 다운로드**
3. Play Console에서 그 서비스 계정에 권한 부여: **재무 데이터 보기** + **주문 및 구독 관리**

**[확인 필요]** 권한 전파에 시간이 걸릴 수 있다(구글 문서상 최대 24시간). 검증
엔드포인트가 403을 주면 이것부터 의심한다.

### 5.3 실시간 개발자 알림(RTDN)

1. Google Cloud → **Pub/Sub → 주제 만들기**
2. 그 주제에 `google-play-developer-notifications@system.gserviceaccount.com`을
   **게시자(Pub/Sub Publisher)** 로 추가
3. Play Console → **수익 창출 설정 → 실시간 개발자 알림** → 주제 이름 입력 →
   **테스트 알림 보내기**
4. Pub/Sub에서 **푸시 구독** 만들기 → 엔드포인트:
   `https://onmyway.olivenrich.com/api/billing/google/rtdn?token=<GOOGLE_RTDN_PUSH_TOKEN>`

토큰은 내가 난수로 만들어 시크릿으로 등록한다(§6). 3번의 테스트 알림이 우리 서버에
`outcome: "test"`로 기록되면 배선이 끝난 것이다.

---

## 6. 우리 쪽 구성 (콘솔이 끝난 뒤 — 내가 한다)

콘솔에서 나온 값들을 서버에 꽂는 단계다. **서비스 계정 JSON은 내가 열지 않는다** —
`wrangler secret put`이 표준입력으로 받으므로 값이 내 눈에 들어오지 않는다.

1. **D1 데이터베이스** — 운영에 `BILLING_DB` 바인딩이 아직 없다. 만들고 바인딩한 뒤
   `migrations/0001`·`0002`를 적용한다.
2. **시크릿 3개**

```bash
npx wrangler secret put GOOGLE_PLAY_SERVICE_ACCOUNT_JSON
```

```bash
npx wrangler secret put GOOGLE_RTDN_PUSH_TOKEN
```

3. **vars**: `GOOGLE_PLAY_PACKAGE_NAME = "com.olivenrich.onmyway"` (비밀이 아니다)
4. 구성이 들어가면 `/api/billing/google/verify`가 503에서 깨어난다. 그 전까지는 두
   엔드포인트 모두 닫혀 있다.

---

## 7. 자세한 설명 최종 문안 (복사해서 붙이기)

```
On My Way는 큰 목표를 오늘 할 수 있는 작은 실행으로 바꾸는 목표 관리 앱입니다.
AI 코치 '올리'가 계획을 다듬고 매일의 실행을 함께 이어갑니다.


■ 계획은 내가 만듭니다

목표, 기간, 실행할 요일과 시간대, 하루에 낼 수 있는 시간을 입력하면
수정 가능한 초안이 채워집니다. 초안은 언제든 직접 고칠 수 있고,
최종 계획은 사용자가 확정합니다.


■ 올리가 하는 일

- 계획 다듬기: 이미 만든 계획을 상황에 맞게 조정합니다
- 회복 계획: 며칠 밀렸을 때 남은 기간에 맞춰 다시 짭니다
- 일정 재배치: 사정이 바뀌면 남은 일정을 옮깁니다
- 대화: 오늘의 실행에 대해 이야기하고 축하와 위로를 건넵니다
- 다이어리 북: 한 달의 기록을 책 형태로 정리합니다

올리의 답변은 참고용입니다. 의료·법률·재정 문제는 전문가와 상의해 주세요.


■ 실행을 돕는 기능

- 오늘의 스케줄: 오늘 해야 할 일만 보여줍니다
- 집중 타이머: 정한 시간 동안 집중하고 완료를 기록합니다
- 기록 열람과 내보내기: 지금까지의 실행 기록을 텍스트(.md) 파일로
  내보낼 수 있습니다. 이 기능은 구독과 관계없이 항상 이용할 수 있습니다


■ 무료 체험과 Pro

가입하면 AI 기능을 쓸 수 있는 무료 체험이 시작됩니다.
카드 등록은 필요하지 않습니다.

Pro 구독: 월 3,900원 (부가세 포함)
- 월 250 에너지 / 하루 30 에너지
- 목표와 계획 개수 제한 없음
- 회복 계획, 일정 재배치, 상세 분석, 올리 개인화
- 다이어리 북 만들기

체험이 끝나거나 구독하지 않아도 기록 열람과 텍스트 내보내기는 계속 됩니다.


■ 데이터에 대해

- 목표와 계획은 로그인한 기기 사이에서 동기화됩니다
- 올리와 나눈 대화는 서버에 저장하지 않고 사용하는 기기에만 남습니다
- 계정 탈퇴는 앱 안에서 직접 할 수 있습니다

자세한 내용은 개인정보 처리방침을 확인해 주세요.
```

초안(`play-store-submission.md` §4)과 다른 곳은 한 줄뿐이다 — Pro 혜택의
"다이어리 북 인쇄와 PDF 저장"을 **"다이어리 북 만들기"** 로 바꿨다.

---

## 순서 요약

1. **[사용자]** 계정 유형 확인 → 나에게 알려준다 (개인이면 비공개 테스트 일정을 같이 짠다)
2. **[사용자]** 앱 만들기 (§1)
3. **[사용자]** 선언 항목 (§2) — 답은 전부 이 문서에 있다. 테스트 계정과 사업자 정보만 직접 입력
4. **[사용자]** 스토어 등록정보 (§3) — 문안·이미지 준비 완료
5. **[사용자]** 내부 테스트 업로드 (§4) — vc2 AAB
6. **[사용자]** 구독 상품·API 액세스·RTDN (§5)
7. **[나]** D1·시크릿·마이그레이션 (§6), 그 뒤 결제 클라이언트 구현
