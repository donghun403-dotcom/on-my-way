# 테스트 규칙

## 부수효과가 있는 경로는 두 불변식을 짝으로 단언한다

외부 부수효과 — provider 호출, 결제 승인, 메일 발송, 원장 재지급 — 가 있는 경로는
**반드시 두 가지를 각각 단언한다**:

| 불변식 | 묻는 것 |
|---|---|
| **회계** | 재화가 두 번 빠지거나 두 번 들어오지 않는가 |
| **부수효과** | 바깥 세계가 두 번 움직이지 않는가 |

둘은 다른 단언이다. 하나만 쓰면 다른 하나가 조용히 깨진다.

### 이 규칙이 생긴 이유 — 같은 구멍에 두 번 빠졌다

**① `shouldExecute`를 무시한 중복 provider 호출**

`worker-energy-ledger.test.mjs`에 "같은 requestId를 동시에 던져도 한 번만 차감된다"가
있었고 통과하고 있었다. 그 테스트는 `spent <= cost`만 봤다.

실제로는 원장이 중복 요청에 `shouldExecute: false`를 돌려주는데 worker가 그 신호를
보지 않고 provider를 다시 불렀다. 크레딧은 정말 한 번만 빠졌다 — 회계 불변식은 참이었다.
유저에게는 공짜였고 우리에게는 실제 API 비용이었다. 부수효과 불변식이 없어서
아무도 몰랐다.

**② 체험 시작 재호출의 원장 재지급**

`/api/ai/trial/start`를 다시 부르면 `startAiTrial`은 올바르게 `started: false`를
돌려줬는데, worker가 그 결과를 보지 않고 원장 `reset`을 실행했다. `resetLedgerForPlan`은
`lastGrantMonthKey`를 비우고 재지급하므로 체험 크레딧이 다시 찼다. 무한 리필이었다.

이 경로에는 **두 불변식 어느 쪽도 없었다.**

### 쓰는 법

```js
// 회계 — 잔량이 그대로인가
assert.equal((await usage(ctx)).balance, spentBefore);

// 부수효과 — 바깥이 다시 움직였는가
assert.equal(await grantTransactionCount(ctx), grantsBefore);
assert.equal(providerCalls, 1);
```

**부수효과는 결과가 아니라 호출로 재라.** "잔량이 안 늘었다"는 "재지급이 없었다"와 다르다 —
재지급과 소비가 같은 양으로 겹치면 잔량은 그대로다. 거래 기록 수, provider 호출 카운터,
`lastGrantMonthKey`처럼 **행위 자체를 남기는 값**을 본다.

### 새 테스트가 실제로 결함을 잡는지 확인하라

수정과 테스트를 같이 쓸 때는 **수정을 잠시 되돌려 테스트가 실패하는 것을 눈으로 보고**
다시 적용한다. 통과만 확인한 테스트는 아무것도 증명하지 않는다. 위 두 결함의 회귀
테스트는 각각 해당 수정 없이 실패하는 것을 확인한 뒤 커밋했다.

## 폐지한 기능은 이름까지 검사한다

정책 값이나 자격을 폐지하면 `plan-policy.test.mjs`의 소스 스캔에 패턴을 추가한다.
식별자 스캔과 화면 문구 스캔은 별개다 — 식별자만 보면 `admin.html`에 남은
`"체험 종료 편지 미열람"` 같은 한글 문구가 빠져나간다. 실제로 그랬다.

`.js`는 문구 스캔에서 제외한다. 폐지를 설명하는 주석과 "이 문구가 없어야 한다"는
음성 단언이 전부 걸리기 때문이다.
