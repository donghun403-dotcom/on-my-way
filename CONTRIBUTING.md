# 테스트 규칙

## 테스트가 실제로 무언가를 증명하게 하는 규칙들

아래 규칙은 전부 같은 가족의 실수에서 나왔다: **통과하는 테스트와 지켜지는 불변식은
다르다.** 각 규칙에는 그것이 생긴 실제 사고가 붙어 있다. 추상적인 모범 사례가 아니라
이 리포지토리에서 실제로 무언가를 놓친 기록이다.

---

### ① 부수효과가 있는 경로는 두 불변식을 짝으로 단언한다

외부 부수효과 — provider 호출, 결제 승인, 메일 발송, 원장 재지급 — 가 있는 경로는
**반드시 두 가지를 각각 단언한다**:

| 불변식 | 묻는 것 |
|---|---|
| **회계** | 재화가 두 번 빠지거나 두 번 들어오지 않는가 |
| **부수효과** | 바깥 세계가 두 번 움직이지 않는가 |

둘은 다른 단언이다. 하나만 쓰면 다른 하나가 조용히 깨진다.

#### 사고 — 같은 구멍에 두 번 빠졌다

**`shouldExecute`를 무시한 중복 provider 호출**

`worker-energy-ledger.test.mjs`에 "같은 requestId를 동시에 던져도 한 번만 차감된다"가
있었고 통과하고 있었다. 그 테스트는 `spent <= cost`만 봤다.

실제로는 원장이 중복 요청에 `shouldExecute: false`를 돌려주는데 worker가 그 신호를
보지 않고 provider를 다시 불렀다. 크레딧은 정말 한 번만 빠졌다 — 회계 불변식은 참이었다.
유저에게는 공짜였고 우리에게는 실제 API 비용이었다. 부수효과 불변식이 없어서
아무도 몰랐다.

**체험 시작 재호출의 원장 재지급**

`/api/ai/trial/start`를 다시 부르면 `startAiTrial`은 올바르게 `started: false`를
돌려줬는데, worker가 그 결과를 보지 않고 원장 `reset`을 실행했다. `resetLedgerForPlan`은
`lastGrantMonthKey`를 비우고 재지급하므로 체험 크레딧이 다시 찼다. 무한 리필이었다.

이 경로에는 **두 불변식 어느 쪽도 없었다.**

#### 쓰는 법

```js
// 회계 — 잔량이 그대로인가
assert.equal((await usage(ctx)).balance, spentBefore);

// 부수효과 — 바깥이 다시 움직였는가
assert.equal(await grantTransactionCount(ctx), grantsBefore);
assert.equal(providerCalls, 1);
```

**부수효과는 결과가 아니라 호출로 재라.** "잔량이 안 늘었다"는 "재지급이 없었다"와 다르다 —
재지급과 소비가 같은 양으로 겹치면 잔량은 그대로다. 거래 기록 수, provider 호출 카운터,
`lastGrantMonthKey`처럼 **행위 자체를 남기는 값**을 본다.

---

### ② 새 테스트가 실제로 결함을 잡는지 확인하라

수정과 테스트를 같이 쓸 때는 **수정을 잠시 되돌려 테스트가 실패하는 것을 눈으로 보고**
다시 적용한다. 통과만 확인한 테스트는 아무것도 증명하지 않는다.

되돌림은 한 가지가 아니라 **고친 것마다** 한다. W2(치어링 상한을 원장으로)에서는 다섯
가지를 각각 되돌려 각각 다른 테스트가 깨지는 것을 확인했다. 그중 하나는 되돌려 보기
전까지 아무도 몰랐던 문제를 드러냈다 — 그것이 아래 ③이다.

---

### ③ 기본값과 같은 값을 단언하는 테스트는 코드가 실행됐음을 증명하지 못한다

단언하는 값이 **"아무것도 하지 않았을 때 나오는 값"과 같으면 그 단언은 0비트다.**
통과해도 해당 코드가 돌았는지 알 수 없다.

#### 사고 — dailyCheer 호환 테스트

W2에서 원장 상태에 `dailyCheer` 필드를 추가하면서 "기존 레코드 호환"을 이렇게 단언했다:

```js
const state = normalizeLedgerState(legacyRecordWithoutDailyCheer, now);
assert.deepEqual(state.dailyCheer, { key: "", celebrate: 0, comfort: 0 }); // 통과
```

**이 테스트는 `normalizeLedgerState`에서 `dailyCheer` 정규화를 통째로 지워도 통과했다.**
`normalizeLedgerState`가 `emptyLedgerState()`를 스프레드한 뒤 필드를 덮어쓰는 구조라,
정규화가 없으면 기본값이 그대로 남는다 — 단언한 값과 정확히 같다. 그래서 "없는 필드를
빈 값으로 채웠다"와 "그 필드를 아예 모른다"가 구분되지 않았다.

②의 되돌림 확인을 하다가 드러났다.

#### 쓰는 법 — 변환·정규화는 최소 세 입력으로 본다

```js
// ① 없음 → 기본값
assert.deepEqual(normalize({}).dailyCheer, { key: "", celebrate: 0, comfort: 0 });

// ② 정상값 → 그대로 살아 돌아온다  ← 이게 빠지면 ①만으로는 0비트다
assert.deepEqual(normalize({ dailyCheer: seeded }).dailyCheer, seeded);

// ③ 망가진 값 → 안전한 값으로 떨어진다
assert.deepEqual(normalize({ dailyCheer: { key: 42, celebrate: "yes" } }).dailyCheer,
                 { key: "42", celebrate: 0, comfort: 0 });
```

같은 이유로, 상태를 심어 두고 동작을 검증하는 테스트는 **심는 값이 기본값과 다른지**부터
확인한다. `energy-ledger-reset.test.mjs`에는 그것만 보는 테스트가 따로 있고, 실제로 한
필드가 기본값 그대로 심겨 있던 것을 잡아냈다.

---

### ④ 상태를 통째로 다시 세우는 함수는 필드별 처분을 목록으로 선언한다

리셋·마이그레이션처럼 상태 레코드 전체를 다시 세우는 함수는 **어느 필드를 지우고 어느
필드를 남기는지**를 코드 옆에 목록으로 선언하고, 테스트가 그 목록의 **망라성과 정확성을
둘 다** 강제한다. 새 필드가 등록되지 않으면 실패하고, 라벨과 실제 동작이 어긋나도 실패한다.

#### 사고 — 같은 형태로 네 번

`resetLedgerForPlan` 하나에서 셋:

1. **체험 무한 리필** — 체험 시작이 이 함수를 불러서 부를 때마다 재지급됐다
2. **멱등성 레코드 소멸** — `state.requests`를 통째로 비워 이미 쓴 requestId가 다시 통했다
3. **dailyCheer 배치** — `state.daily` 안에 뒀다면 정당한 전환 한 번에 오늘 응원이 되살아났다

그리고 원장 밖에서 하나 더 — **이쪽은 실제 유저 데이터가 사라졌다**:

4. **로그인 시 계정 스냅샷 복원이 게스트 계획을 덮었다.** `switchAccountStorageScope`가
   "계정 스냅샷이 비어 있을 때만" 물어보고, 비어 있지 않으면 아무것도 묻지 않고 계정
   스냅샷을 복원했다. 로그아웃 중에 만든 계획은 익명 스냅샷에 남아 있었지만 화면에서는
   사라졌다. **두 번 고쳤다** — 처음 고친 뒤에도 승계 확인 경로에 같은 형태가 남아 있었다.
   지금은 어느 쪽도 버리지 않고 표식만 남긴 뒤 인앱 시트로 고르게 한다.

넷 다 "복원·리셋이 **무엇을 덮고 무엇을 남기는가**"를 아무도 명시적으로 답하지 않아서
생겼다. 상태를 통째로 다시 세우는 코드에서 반복해서 나오는 형태이고, 저장소가 원장이든
`localStorage`든 같다. 새 필드나 새 스냅샷 키를 추가할 때 그 질문이 자동으로 던져지지
않으면 다섯 번째가 온다.

구현은 `energy-ledger.mjs`의 `RESET_DISPOSITION`과 `energy-ledger-reset.test.mjs`.
클라이언트 스냅샷 쪽은 아직 같은 목록이 없다 — 있어야 할 자리다.

---

## 폐지한 기능은 이름까지 검사한다

정책 값이나 자격을 폐지하면 `plan-policy.test.mjs`의 소스 스캔에 패턴을 추가한다.
식별자 스캔과 화면 문구 스캔은 별개다 — 식별자만 보면 `admin.html`에 남은
`"체험 종료 편지 미열람"` 같은 한글 문구가 빠져나간다. 실제로 그랬다.

`.js`는 문구 스캔에서 제외한다. 폐지를 설명하는 주석과 "이 문구가 없어야 한다"는
음성 단언이 전부 걸리기 때문이다.
